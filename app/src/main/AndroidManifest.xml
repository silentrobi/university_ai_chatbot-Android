<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.karabukaiassistant">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:hardwareAccelerated="true"
        android:icon="@mipmap/launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.example.karabukaiassistant.view.ChatActivity" />
        <activity android:name="com.example.karabukaiassistant.view.RegistrationActivity" />
        <activity android:name="com.example.karabukaiassistant.view.NewPasswordActivity" />
        <activity android:name="com.example.karabukaiassistant.view.UserAccountSecurityActivity" />
        <activity android:name="com.example.karabukaiassistant.view.LoginActivity" />
        <activity android:name="com.example.karabukaiassistant.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>